# DaCapo 2006 Local Inputs

The benchmark jars, dependency jars, Tamiflex reflection logs, and auxiliary
files in this directory were copied locally from:

```text
D:\gitdesk\qilin-generics\artifact\benchmarks\dacapo2006
```

They are intentionally ignored by Git. The checked-in Gradle task
`runDacapo2006` uses the nine programs defined by the original Qilin artifact
configuration in `artifact/util/bm2006.py`.
